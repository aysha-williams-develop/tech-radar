// import readCsvFile from './csv-retriever.js';
// const readCsvFile = require('./csv-retriever.js')

test('it reads a csv file', () => {
  // expect(readCsvFile("csv-retriever.test.csv").length).not.toBe(0);
});